# A5 (previews/status/indicators) + A6 (sidebar/window) batch — Plan

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development. `- [ ]` steps.

**Goal:** A5 — a Resume preview + honest status/indicators + educational empty states. A6 — a collapsible, persisted sidebar + window-state restore. Refine what exists; petrol/ivory/tangerine; opacity/transform only; reduced-motion respected; no redesign.

## Reality checks (from inspection)
- **Connector version is NOT available** (hub tracks only PROTOCOL_VERSION, not a per-connector app version). Do "last seen" only; do NOT invent a version. (Matches the user's "version only when useful".)
- **git-dirty** (`git_status.dirty`/`changedCount`) drives the unsaved-changes dot.
- **`humanizeCapsule(r)`** already returns `{ icon, summary, savedAgo }` per resource — reuse for the preview + "saved X ago".
- **"Last session 2h 17m"** needs session tracking → **Track B**; the preview shows tools+counts+"saved X ago" for now.
- **Project reordering** needs a DB sort field → **Track B** (deferred).

## Global Constraints
- Presentation-first. Reuse existing invokes (`task_resources`, `git_status`, store connectors). The ONE backend-ish addition is the first-party `tauri-plugin-window-state` (window bounds persistence — purpose-built, expected for "remember window state"). No product/data command added.
- Compose from `@/components/ui/*` (Popover/HoverCard, Tooltip, Badge, EmptyState, Skeleton). Token-only; title-case; reduced-motion safe. cargo: `export PATH="$HOME/.cargo/bin:$PATH"`. Repo root `/Users/sammy/omnibus`, node v18.20.

---

### A5-T1 — Resume preview
**Files:** `pages/CapsulesPage.tsx` (+ maybe a small `restore/ResumePreview.tsx`), reuse `lib/humanize.ts`.
- Give each task a lightweight **preview** of its capsule contents so the user has confidence BEFORE resuming — WITHOUT adding friction to the instant Resume. Implementation: a `Popover` (or hover `Tooltip`/HoverCard) triggered by an unobtrusive affordance on the task row (e.g. the capsule-summary text becomes a hover/click trigger, or a small "info"/chevron). On open, show the per-resource breakdown from `resources[taskId]` via `humanizeCapsule`: each tool row = icon + friendly name + summary (e.g. "VS Code — 3 files · 2 terminals", "Chrome — 12 tabs", "Git — on main") + a muted "saved {savedAgo}". If no capsule: "No saved state yet." Header line: "Saved state".
- Do NOT block/precede the Resume button with a modal — the preview is a peek, Resume stays one click. Keep it calm; reduced-motion fine (Popover already animates via shadcn classes).
- Tests: a task with a resource → opening the preview shows the humanized per-tool breakdown + saved-ago; a task with none → "No saved state yet". Non-flaky. Build+test green. Commit `feat(ui): Resume preview — peek a capsule's contents before resuming`.

---

### A5-T2 — Status & indicators + educational empty states
**Files:** `pages/ProjectsPage.tsx` (unsaved dot), `views/GitLine.tsx` (source of dirty — reuse), `pages/ConnectorsPage.tsx` (last-seen), the pages' empty states.
- **Unsaved-changes dot:** on each project card header, a subtle **orange** dot (use `bg-warning` or `bg-primary`? — use a restrained amber `bg-warning`) when the project's git is **dirty** (uncommitted changes). Source the dirty state from `git_status` (`dirty`/`changedCount`). Avoid double-fetch: EITHER lift git status to the project card and pass into GitLine, OR do one small `git_status` fetch at the card level for the dot (GitLine keeps its own — acceptable minor duplication; prefer lifting if clean). Tooltip on the dot: "{n} uncommitted change(s)". Subtle, not distracting (small dot, not a banner).
- **Connector last-seen:** in ConnectorsPage, show "Last seen {relativeTime(connectedSince)}" for disconnected connectors and "Connected · since {relativeTime}" for connected — using `relativeTime` from `@/lib/humanize`. Do NOT add a version line (not available). Keep the icon+text status (a11y) from before.
- **"Saved X ago":** ensure each capsule/task shows `savedAgo` (from humanizeCapsule) — it likely already does; verify + make consistent.
- **Educational empty states:** refine the existing `EmptyState`s across pages so each TEACHES (per the user's "every empty state should teach"): e.g. Projects empty → "No projects yet. Register your first project and Rabta will remember your entire workflow — files, branches, tabs, and terminals." + primary CTA. Capsules empty → what a capsule is. Connectors empty → how to connect (already names extensions). Keep them calm, on-brand, one primary action each.
- Tests: dirty project shows the dot (mock git_status dirty); connector shows last-seen text; empty states render their teaching copy + CTA. Build+test green. Commit `feat(ui): unsaved-changes dot, connector last-seen, educational empty states`.

---

### A6-T1 — Collapsible sidebar (persisted)
**Files:** `shell/Sidebar.tsx`, `shell/AppShell.tsx`, `store.ts` (persisted flag), maybe `App.tsx` (shortcut).
- Add a **collapse toggle** (a small button at the sidebar bottom or top; also a shortcut **⌘\\** or **⌘B**). Collapsed → the sidebar narrows to an icons-only rail (~56px): nav rows show just their icon (label hidden), with a `Tooltip` (side="right") giving the label; the "Local only" badge collapses to its dot (tooltip keeps the text); the sidebar logo stays (mark only when collapsed). Expanded → current 220px full labels.
- **Animate** the width smoothly (`transition-[width] duration-200 ease-brand` on the sidebar + the AppShell grid column — grid `grid-cols-[var(--sidebar-w)_1fr]` or conditional `grid-cols-[56px_1fr]`/`[220px_1fr]`). Content (labels) fade/slide. Reduced-motion neutralizes.
- **Persist** the collapsed state so it survives reloads: add `sidebarCollapsed: boolean` + `toggleSidebar()` to the store, persisted to `localStorage` (a tiny persist wrapper, or read/write localStorage in the store's action + init). Initialize from localStorage on load.
- Keep AppShell's grid/traffic-light/titlebar correct at both widths (the titlebar left cell still clears the lights regardless).
- Tests: toggle flips the state + persists (localStorage set); collapsed hides labels (icons+tooltips remain); shortcut toggles. Non-flaky. Build+test green. Commit `feat(shell): collapsible sidebar with persisted state (⌘\\)`.

---

### A6-T2 — Window-state persistence
**Files:** `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/src-tauri/src/lib.rs`, `apps/desktop/package.json` (maybe the JS plugin, optional).
- Add the official **`tauri-plugin-window-state`** so the window's size + position are restored between launches (the user asked to "remember window state between launches"). Add the crate to `Cargo.toml` (version matching tauri 2), register it in the builder: `.plugin(tauri_plugin_window_state::Builder::default().build())` in `lib.rs`'s `tauri::Builder`. (The plugin auto-saves on close and restores on start; no JS needed for basic size/position. Add the JS `@tauri-apps/plugin-window-state` only if a capability/permission requires it — check the plugin's v2 setup; if a permission is needed, add it to the capabilities/allowlist.)
- Respect the existing `minWidth`/`minHeight` (760×540) — the plugin clamps to constraints. Keep `titleBarStyle: Overlay` + `trafficLightPosition`.
- Verify: `cargo build` clean (plugin compiles + registers). Live: resize/move the window, quit, relaunch → it reopens at the last size/position. Commit `feat(app): persist window size & position between launches (window-state plugin)`.

---

## DoD (batch)
- Resume preview peeks a capsule's contents (tools+counts+saved-ago) before resuming, no added Resume friction.
- Unsaved-changes dot on dirty projects; connector last-seen (no fake version); "saved X ago" consistent; empty states teach.
- Sidebar collapses to an icon rail (tooltips), animates, and remembers its state; window reopens at last size/position.
- Reduced-motion + keyboard accessible; token-only; no redesign; `pnpm --filter desktop test` + `cargo build` green; live-verified.

## Deferred to Track B (per reality checks)
- Session duration in the Resume preview ("Last session 2h 17m").
- Project reordering (needs a DB sort field).
- Connector version (needs the hub to capture a connector-reported version).
