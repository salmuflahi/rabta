# A2 (finish) + A3 batch — Nav cleanup, Microinteractions, Keyboard & Command Palette

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development. `- [ ]` steps.
> User directive (2026-07-22): the nav needs work — a faint native "Rabta" title overlaps the ⌘K search; traffic lights aren't aligned; and the logo appears TWICE (titlebar + sidebar top, stacked). Fold the nav fixes in, then do the next TWO phases at once (finish A2 microinteractions + A3 keyboard/command-palette).

## Global Constraints
- Presentation only. No `invoke`/event/payload change. (Nav-fix touches window chrome config only.)
- Petrol/ivory/tangerine; opacity/transform/shadow only; `prefers-reduced-motion` respected; compose from `@/components/ui/*`; title-case; no new deps. cargo: `export PATH="$HOME/.cargo/bin:$PATH"`. Repo root `/Users/sammy/omnibus`, node v18.20.

---

### T1 — Nav cleanup (fixes the user's complaints)
**Files:** `apps/desktop/src-tauri/tauri.conf.json`, `apps/desktop/src/shell/Titlebar.tsx`, `apps/desktop/src/shell/Sidebar.tsx` (minor).
- **Kill the ghost title:** the native overlay title bar still paints the window title text ("Rabta") faintly in the center, over the ⌘K search. Set the window `title` to `""` (empty) in tauri.conf so no native title text renders. (productName "Rabta" stays for the app/menu bar; only the per-window title string is blanked.)
- **Single logo:** the Rabta mark+wordmark appears BOTH in the Titlebar left AND at the Sidebar top (stacked, redundant). Since the traffic lights now occupy the titlebar's top-left, **remove the mark+wordmark from `Titlebar.tsx`** entirely. The titlebar-left cell becomes empty drag space that simply reserves room for the traffic lights. Keep the SINGLE brand logo in the Sidebar (unchanged). Result: lights → ⌘K (center) → status (right) in the bar; one logo, in the sidebar.
- **Titlebar layout after logo removal:** keep the 3-col grid but the left cell is now just a spacer that clears the lights (~`pl-[72px]` worth of width / an empty `div` sized so ⌘K stays visually centered). Ensure the ⌘K button stays centered and the right status chip stays right. Keep `data-tauri-drag-region` on the header; keep ⌘K + status clickable.
- **Light alignment:** the lights weren't vertically aligned with the (now-removed) logo; with the logo gone, just center them in the 58px bar. Current `trafficLightPosition {x:19,y:21}` — keep, adjustable by the controller live.
- **Verify:** `cargo build` clean; `pnpm --filter desktop test` green (update Titlebar test if it asserted the wordmark); `tsc -b` clean. Controller live-verifies (one bar, no ghost title, one logo, lights centered, drag/zoom/clicks work). Commit `fix(shell): nav cleanup — remove duplicate logo, blank ghost window title`.

---

### T2 — Interactive microinteractions (A2)
**Files:** `components/ui/button.tsx`, `card.tsx`, `shell/Sidebar.tsx`, `components/ui/input.tsx`, maybe `index.css` (soft-shadow token).
- **Button:** base gets `active:scale-[0.98]` + `transition-[transform,background-color,box-shadow] duration-150`; keep focus ring + variants/API. Restrained.
- **Card:** soften default shadow; add an OPTIONAL interactive hover (a utility used by clickable cards — `hover:shadow-md transition-shadow` or a subtle `hover:-translate-y-px`), NOT applied to static cards. Pick one, tasteful.
- **Sidebar nav:** active pill + hover already use `transition-colors`; add a slightly smoother feel (keep it). Ensure hover/active read on petrol.
- **Inputs:** `transition-[box-shadow,border-color]` for a smooth focus.
- **Soft shadows:** if `shadow-sm/md` read harsh on ivory, add `--shadow-soft` and use it on cards/sheet.
- Class-only; reduced-motion auto-safe. Build+test green. Commit `feat(ui): calm button/card/sidebar/input microinteractions`.

---

### T3 — Overlay motion + loading skeletons (A2)
**Files:** verify `dialog/dropdown-menu/context-menu/tooltip/popover/command.tsx` enter/exit; add skeletons to `pages/{Capsules,Projects,Connectors,Activity,Overview}Page.tsx`.
- **Overlay motion:** the shadcn `data-[state=open]:animate-in ...` classes exist — verify they fade+zoom; tune to brand ease (~150–180ms, subtle). Don't touch the Restore sheet (own motion). sonner toaster: confirm natural slide/fade.
- **Skeletons:** each data page shows nothing/empty-flash before its first fetch resolves. Add a per-page `loading` state (local `useState`, false after first fetch settles) rendering 2–4 `Skeleton` rows/cards while true. Keep the existing empty states for loaded-but-zero. Presentational; wraps existing fetches; no new invoke.
- Update/adjust page smoke tests (render loading→content; no throw). Build+test green. Commit `feat(ui): overlay enter/exit motion + loading skeletons`.

---

### T4 — Command palette → Raycast-style search (A3)
**Files:** `shell/CommandPalette.tsx`, maybe a small `lib/search.ts`.
- Today ⌘K jumps to nav sections. Expand to fuzzy-search across: **nav sections**, **projects** (→ open Projects / that project), **capsules/tasks** (→ Capsules; and a "Resume {task}" action per task), **connectors** (→ Connectors), **settings**, and **actions** (Register project, New task, Toggle theme, Open Privacy/Settings). Group results by type with headings (cmdk supports groups). Use the store's `projects` + fetch tasks as needed (reuse existing invokes: `list_projects`, `list_tasks` — the palette can read what's already in the store/pages; if it needs tasks, fetch via existing `list_tasks` per project on open, cached). Fuzzy match on label (cmdk's built-in filter is fine, or a tiny scorer). Selecting an item runs its action (setView / start a flow) and closes.
- Keep it fast + calm; keyboard-first (arrow/enter/esc already from cmdk). Reduced-motion fine.
- Tests: palette opens, typing filters, selecting a nav item calls setView; a "Resume" action item exists for a seeded task. Non-flaky. Build+test green. Commit `feat(ui): Raycast-style command palette (fuzzy search projects/tasks/connectors/actions)`.

---

### T5 — Global keyboard shortcuts + nav (A3)
**Files:** `App.tsx` (or a `useHotkeys` hook in `shell/`), `pages/CapsulesPage.tsx`/`ProjectsPage.tsx` for the action hooks, store if needed.
- Global shortcuts (a single keydown handler at the shell, cleaned up on unmount; ignore when typing in an input/textarea/contenteditable, EXCEPT ⌘K which always works):
  - **⌘K** → open command palette (already wired — keep).
  - **⌘N** → New Project (open the Projects register dialog; navigate to Projects + trigger it).
  - **⌘⇧N** → New Capsule/Task (navigate to Capsules + focus the new-task input, or open a create affordance).
  - **⌘R** → Resume last (re-run the Restore for the most-recently-active task if one exists; if none, no-op or a gentle toast). Reuse the Capsules resume path — expose a store field for "last resumable task id" (the `activeTaskId` or last resumed) so ⌘R can trigger it.
  - (Guard: don't clobber the browser/webview reload on ⌘R — in the Tauri app ⌘R isn't reload by default in production, but in dev vite may; `preventDefault` and scope to our handler.)
- Ensure Tab/arrow keyboard navigation works across the sidebar + page controls (focus-visible already there; verify no focus traps).
- Tests: simulate ⌘N/⌘⇧N/⌘R keydown → asserts the right view/action; typing in an input doesn't trigger them. Non-flaky. Build+test green. Commit `feat(ui): global keyboard shortcuts ⌘N/⌘⇧N/⌘R + keyboard nav`.

---

## DoD (batch)
- Nav: one bar, integrated centered traffic lights, NO ghost title, ONE logo (sidebar); drag/zoom/clicks work.
- Calm microinteractions (button/card/sidebar/input); overlays fade/scale; toasts natural; data pages show skeletons.
- ⌘K is a real fuzzy command palette over projects/tasks/connectors/settings/actions; ⌘N/⌘⇧N/⌘R work; keyboard nav sound.
- Reduced-motion respected; petrol/ivory/tangerine; no redesign; `pnpm --filter desktop test` + `cargo build` green; live-verified.

## Deferred to later phases (per user)
- Deeper nav rework beyond the duplicate-logo/ghost-title fix (fold into A6 sidebar phase).
